# Task Completion Checklist

Since no linting, formatting, or testing tools are configured:

1. Verify code runs without errors (if applicable)
2. Check that JSON files remain valid JSON after edits
3. Ensure new investigation nodes follow the ID convention (ENT-xxx, RED-xxx, etc.)
4. Update investigation_log.md if significant findings or actions taken
